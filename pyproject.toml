[project]
name = "data-analyst-agent-chat-assistant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain-google-genai>=4.2.1",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
